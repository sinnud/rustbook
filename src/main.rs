mod collatz;

/** # main function executable program start from.
 
- call functions defined in other file (mod), imported using `mod [mod_name]`.

 */
fn main() {
    //collatz::main();
    collatz::reverse_collatz();
}